import React from 'react';
import Link from 'next/link';
import { LinkSimple, Minus } from '@phosphor-icons/react/dist/ssr';
import { HowToScroll } from '../../components/blocs/HowToScroll';
import { Motion } from '../../components/motion/Motion';
import {
    variantsOne,
    variantsTwo,
    variantsThree,
    variantsFour
} from '../../components/motion/variants';
import { AnimatedNameLink } from '../../components/text/AnimatedName.tsx';
import { Badge } from '../../components/ui/Badge';
import { Button } from '../../components/ui/Button';
import { Separator } from '../../components/ui/Separator';
import { wefix } from './WeFix';
import { spinalCom } from './SpinalCom';
import { economat } from './EdA';

export const metadata = {
    title: "Entreprises pour lesquelles j'ai travaillé",
    alternates: {
      canonical: '/work',
    },
};

export const Small = ({ children }) => (
    <small className="flex items-center gap-x-2 text-sm text-theme font-bold -mt-3">
        <Minus weight="bold" /> {children}
    </small>
);

export const Strong = ({ children }) => (
    <strong className="text-theme font-bold -mt-3">
        {children}
    </strong>
);

# Expériences professionnelles

<AnimatedNameLink />

<Motion className="mt-10" variants={variantsOne} asChild>
    Je travaille actuellement chez **WeFix**, une entreprise leader dans la **réparation** des
    appareils électroniques, que j’ai rejoint en **2020** en tant que **développeur web**.
    Depuis mon arrivée, j’ai participé à de nombreux projets visant à améliorer les plateformes
    en ligne et les services numériques de l’entreprise.
    Au fil de ma carrière, j’ai eu l’occasion de collaborer avec des
    entreprises de tailles variées, des **startups** aux **grandes entreprises**.
</Motion>

<div className="mt-14">
    <Motion className="!text-2xl sm:!text-3xl" variants={variantsTwo} asChild>
        ## WeFix by Fnac
    </Motion>
    <Motion variants={variantsTwo} asChild>
        <Small>Développeur web (jan. 2020 - aujourd'hui)</Small>
    </Motion>
    <Motion className="mt-3" variants={variantsTwo} asChild>
        <div className="flex items-center gap-x-2">
            <Badge>WeFix</Badge>
            <Badge>Entreprise</Badge>
            <Badge>CDI</Badge>
        </div>
    </Motion>
    <Motion className="mt-6" variants={variantsTwo} asChild>
        J'ai rejoint **WeFix (groupe Fnac & Darty)** en **2020**, en tant que développeur web.
        J'ai eu la chance de travailler sur des projets variés, de la refonte du site web
        à la mise en place de nouvelles fonctionnalités, en interne comme chez de nouveaux
        partenaires comme **Bouygues Telecom**, **Garantie Privée**, **Assurant**, **Fnac**, etc.
    </Motion>
    <Motion className="mt-6 text-sm" variants={variantsTwo} asChild>
        <Strong>Technologies utilisées:</Strong> React, Next.js, Tailwind CSS,
        TypeScript, Express, MongoDB, Fastify, JavaScript, Sass, CSS, Pug etc.
    </Motion>
    <Motion className="mt-3" variants={variantsTwo} asChild>
        <div className="scrollbar-hide flex h-14 w-full flex-row space-x-2 overflow-x-auto">
            {wefix.map(({ icon, title }, index) => (
                <div
                    key={index}
                    className="flex aspect-square items-center justify-center rounded-md border border-neutral-200 bg-neutral-50 px-3 py-4 dark:border-neutral-700 dark:bg-neutral-800"
                >
                    {icon}
                    <p className="sr-only">{title}</p>
                </div>
            ))}
        </div>
    </Motion>
    <Motion className="mt-3" variants={variantsTwo} asChild>
        <HowToScroll>
            Vous pouvez scroller de{' '}
            <span className="font-medium text-theme">gauche</span> à{' '}
            <span className="font-medium text-theme">droite</span> pour voir toutes
            les technologies et langages que j'utilise et que j'ai consolidés dans cette entreprise.
        </HowToScroll>
    </Motion>
    <Motion className="mt-6" variants={variantsTwo} asChild>
        <Button size="lg" variant="outline" className="flex items-center gap-x-2 font-semibold">
            <Link
                href="https://wefix.net/"
                aria-label="Site web de WeFix"
                target="_blank"
                rel="noreferrer noopener"
                className="flex items-center gap-x-2 font-semibold">
                <LinkSimple className="size-4 shrink-0" />
                Site web de WeFix
            </Link>
        </Button>
    </Motion>

    <Separator className="my-14" />

    <Motion className="!text-2xl sm:!text-3xl" variants={variantsThree} asChild>
        ## SpinalCom
    </Motion>
    <Motion variants={variantsThree} asChild>
        <Small>Designer Web (juil. 2019 - jan. 2020)</Small>
    </Motion>
    <Motion className="mt-3" variants={variantsTwo} asChild>
        <div className="flex items-center gap-x-2">
            <Badge>SpinalCom</Badge>
            <Badge>Startup</Badge>
            <Badge>Alternance</Badge>
        </div>
    </Motion>
    <Motion className="mt-6" variants={variantsThree} asChild>
        Durant ma **dernière année d'alternance**, j'ai rejoint **SpinalCom**,
        une startup spécialisée dans le **développement** de solutions pour le **bâtiment connecté**.
        J'ai eu l'opportunité de travailler sur un dashboard de gestion des équipements connectés,
        la **refonte du site web** et la **création de maquettes** pour de nouveaux projets.
    </Motion>
    <Motion className="mt-6 text-sm" variants={variantsThree} asChild>
        <Strong>Technologies utilisées:</Strong> Vue, Vuetify, JavaScript, TypeScript, HTML,
        CSS, Sass, Webpack, MongoDB, PostgreSQL etc.
    </Motion>
    <Motion className="mt-3" variants={variantsThree} asChild>
        <div className="scrollbar-hide flex h-14 w-full flex-row space-x-2 overflow-x-auto">
            <div className="scrollbar-hide flex h-14 w-full flex-row space-x-2 overflow-x-auto">
                {spinalCom.map(({ icon, title }, index) => (
                    <div
                        key={index}
                        className="flex aspect-square items-center justify-center rounded-md border border-neutral-200 bg-neutral-50 px-3 py-4 dark:border-neutral-700 dark:bg-neutral-800"
                    >
                        {icon}
                        <p className="sr-only">{title}</p>
                    </div>
                ))}
            </div>
        </div>
    </Motion>
    <Motion className="mt-3" variants={variantsThree} asChild>
        <HowToScroll>
            Vous pouvez scroller de{' '}
            <span className="font-medium text-theme">gauche</span> à{' '}
            <span className="font-medium text-theme">droite</span> pour voir toutes
            les technologies et langages que j'utilise et que j'ai consolidés dans cette entreprise.
        </HowToScroll>
    </Motion>
    <Motion className="mt-6" variants={variantsThree} asChild>
        <Button size="lg" variant="outline" className="flex items-center gap-x-2 font-semibold">
            <Link
                href="https://www.spinalcom.com/en/"
                aria-label="Site web de SpinalCom"
                target="_blank"
                rel="noreferrer noopener"
                className="flex items-center gap-x-2 font-semibold">
                <LinkSimple className="size-4 shrink-0" />
                Site web de SpinalCom
            </Link>
        </Button>
    </Motion>

    <Separator className="my-14" />

    <Motion className="!text-2xl sm:!text-3xl" variants={variantsFour} asChild>
        ## Économat des Armées
    </Motion>
    <Motion variants={variantsFour} asChild>
        <Small>Développeur web (jan. 2018 - juil. 2019)</Small>
    </Motion>
    <Motion className="mt-3" variants={variantsFour} asChild>
        <div className="flex items-center gap-x-2">
            <Badge>Économat des Armées</Badge>
            <Badge>Startup</Badge>
            <Badge>Alternance</Badge>
        </div>
    </Motion>
    <Motion className="mt-6" variants={variantsFour} asChild>
        Durant ma **première année d'alternance**, j'ai rejoint **l'Économat des Armées**,
        une entreprise partenaire du soutien des **forces armées**.
        J'ai travaillé sur la **création d'un site** sur l'intranet du **Ministère des Armées**,
        et d'un **annuaire interne** à l'entreprise.
    </Motion>
    <Motion className="mt-6 text-sm" variants={variantsFour} asChild>
        <Strong>Technologies utilisées:</Strong> Python, Flask, React, HTML, CSS, JavaScript, Parcel, Bootstrap etc.
    </Motion>
    <Motion className="mt-3" variants={variantsFour} asChild>
        <div className="scrollbar-hide flex h-14 w-full flex-row space-x-2 overflow-x-auto">
            {economat.map(({ icon, title }, index) => (
                <div
                    key={index}
                    className="flex aspect-square items-center justify-center rounded-md border border-neutral-200 bg-neutral-50 px-3 py-4 dark:border-neutral-700 dark:bg-neutral-800"
                >
                    {icon}
                    <p className="sr-only">{title}</p>
                </div>
            ))}
        </div>
    </Motion>
    <Motion className="mt-6" variants={variantsThree} asChild>
        <Button size="lg" variant="outline" className="flex items-center gap-x-2 font-semibold">
            <Link
                href="https://www.economat-armees.com/"
                aria-label="Site web de l'Économat"
                target="_blank"
                rel="noreferrer noopener"
                className="flex items-center gap-x-2 font-semibold">
                <LinkSimple className="size-4 shrink-0" />
                Site web de l'Économat
            </Link>
        </Button>
    </Motion>
</div>
