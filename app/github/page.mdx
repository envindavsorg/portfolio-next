import React from 'react';
import { Suspense } from 'react';
import { GitHubStatsServer } from '@/components/github/stats/GitHubStatsServer';
import { ContributionsGraphServer } from '@/components/github/contributions/ContributionsServer';
import { ContributionsTableServer } from '@/components/github/contributions/ContributionsServer';
import { Spinner } from '@/components/ui/Spinner';
import { Motion } from '@/components/motion/Motion';
import {
    variantsOne, variantsTwo, variantsThree,
} from '@/components/motion/variants';
import { AnimatedName } from '@/app/animated-name.tsx';
import { HowToScroll } from '@/components/blocs/HowToScroll';

export const metadata = {
    title: 'Activité et statistiques de mon profil GitHub',
    alternates: {
        canonical: '/github',
    },
};

# Activité et statistiques de mon profil GitHub

<AnimatedName />

<Motion className="mt-12" variants={variantsOne} asChild>
    Toutes mes statistiques GitHub (nombre de commits, abonnés, suivis)
    de l'année {new Date().getFullYear() - 1} - {new Date().getFullYear()} sont affichées ici.
</Motion>

<Suspense fallback={<Spinner className="h-52" />}>
    <Motion className="mt-4" variants={variantsOne}>
        <GitHubStatsServer />
    </Motion>
</Suspense>

<Motion className="mt-12" variants={variantsTwo} asChild>
    Mon graphique de contributions GitHub sur l'année dernière
</Motion>
<Motion className="mt-4" variants={variantsTwo}>
    <ContributionsGraphServer />
</Motion>

<Motion className="mt-12" variants={variantsThree} asChild>
    Plus de détails sur mon nombre de commits de la semaine dernière
</Motion>
<Motion className="mt-4" variants={variantsThree}>
    <ContributionsTableServer />
</Motion>
